{
  "name": "keepding",
  "version": "1.0.0",
  "description": "A fast, offline-first personal library and memo board for Chrome (inspired by Google Keep + Linkding)",
  "type": "module",
  "scripts": {
    "dev": "vite build --watch",
    "build": "tsc && vite build",
    "test": "tsx --test tests/services.test.ts",
    "package": "npm run build && rm -f keepding-extension.zip && (cd dist && zip -r ../keepding-extension.zip . -x \"*.DS_Store\")"
  },
  "dependencies": {
    "idb": "^8.0.2",
    "marked": "^15.0.7"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.308",
    "@types/node": "^22.13.9",
    "autoprefixer": "^10.4.20",
    "postcss": "^8.5.3",
    "tailwindcss": "^3.4.17",
    "tsx": "^4.23.13",
    "typescript": "^5.8.2",
    "vite": "^6.2.0"
  }
}
